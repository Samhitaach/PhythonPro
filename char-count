wds=input("Enter any string =")
d={}
for x in wds:
# To get key and default value 0
   d[x]= d.get(x,0)+1
     # To print key and value in tuple form using items() and It sorts characters in lexicographic (alphabetical) order. 
for i, j in sorted(d.items()):
    print(i , j)
