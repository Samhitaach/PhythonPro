Technically, in Python, an iterator is an object which implements the iterator protocol, which consist of the methods __iter__() and __next__().
Implemented using list, tuple and more

tup=(1,6,8)
pr=iter(tup)
print(next(pr))

mystr = "banana"
myit = iter(mystr)

print(next(myit))
print(next(myit))
